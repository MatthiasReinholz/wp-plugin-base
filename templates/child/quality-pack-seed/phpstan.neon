# Child-owned PHPStan overlay loaded after phpstan.neon.dist.
#
# Add repo-specific bootstrapFiles, scanFiles, or exclusions here when the
# managed WordPress quality pack is enabled.
#
# Example:
# parameters:
#   bootstrapFiles:
#     - vendor/autoload.php

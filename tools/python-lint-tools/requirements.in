codespell==2.4.3
yamllint==1.38.0
# Patch the installer used by lint-only and combined tool environments.
pip==26.2
# Python 3.10/3.11 venvs also seed setuptools; replace vulnerable bundled copies.
setuptools==84.0.0
